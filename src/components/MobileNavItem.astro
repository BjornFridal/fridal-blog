---
interface Props {
  isActive?: boolean;
  href?: string;
}

const { href, isActive } = Astro.props;
---

<li>
  <a
    href={href}
    class:list={[
      'block px-3 py-2 transition',
      isActive ? 'text-teal-500' : 'hover:text-teal-500'
    ]}
  >
    <slot />
  </a>
</li>
