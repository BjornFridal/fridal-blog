---
interface Props {
  dateTime: string;
  class: string;
}

const { dateTime, class: className } = Astro.props;
---

<time
  datetime={dateTime}
  class:list={[
    'relative z-10 mb-3 text-sm text-zinc-400 dark:text-zinc-500',
    className
  ]}
>
  <slot />
</time>
